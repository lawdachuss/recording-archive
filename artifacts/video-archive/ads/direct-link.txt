<!--
  direct-link.txt — Smartlink / Direct Link URLs (CrakRevenue direct links)

  Paste ONE URL PER LINE, e.g.
      https://your-affiliate-link.example/path

  The site picks one at random for text CTA links (the smartlink used on
  the Premium page's rewarded-view button and other rewarded-view CTAs).
  Empty file = those CTAs open nothing until a link is pasted here.

  Separate multiple links with a line containing only: --- so some links
  show more often (each --- block counts once).

  Paste starting below this comment.
-->
